# Week 03 · Day 01 — OOP basics

**Estimated time:** ~5 hrs
**GitHub issue:** (link the Day-01 issue here)

## Objectives
- [ ] Define a class with `__init__`, instance attributes, and methods.
- [ ] Create instances and call methods; explain `self`.
- [ ] Recognize when a class is a better model than a bunch of loose variables/dicts.

## Learn (with resources)
- **CS50P** — Week 8 (Object-Oriented Programming) lecture.
- **Corey Schafer** — "Python OOP Tutorial 1: Classes and Instances" (YouTube).
- **Real Python** — "Object-Oriented Programming (OOP) in Python 3": https://realpython.com

## Homework (branch → PR)
1. [ ] Branch: `git switch -c day-01-oop-basics`.
2. [ ] `projects/small/oop/bank_account.py`: a `BankAccount` class with `__init__(self, owner, balance=0)`, methods `deposit(amount)` and `withdraw(amount)` (reject negative amounts and over-withdrawals), and a `balance` attribute. Add docstrings.
3. [ ] `projects/small/oop/rectangle.py`: a `Rectangle` class with `width`/`height` and methods `area()` and `perimeter()`.
4. [ ] In an `if __name__ == "__main__":` block, create a couple of instances and demonstrate every method, printing results.
5. [ ] CS50P Week 8 problem set: complete at least one exercise (e.g. `Jar`).
6. [ ] Push, PR with `Closes #<issue>`, self-review, merge.

## Reflection
- **What clicked:**
- **What's still fuzzy:**
- **One thing to review tomorrow:**
- **Time actually spent:**
