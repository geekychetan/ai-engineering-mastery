# Week 02 · Day 02 — Lists & tuples deep + intro comprehensions

**Estimated time:** ~5 hrs
**GitHub issue:** (link the Day-02 issue here)

## Objectives
- [ ] Use list methods fluently (`.append()`, `.extend()`, `.insert()`, `.pop()`, `.remove()`, `.sort()` vs `sorted()`).
- [ ] Explain list vs tuple (mutability, when to use each) and work with nested lists.
- [ ] Write basic list comprehensions (with an `if` filter) and know when a loop reads better.

## Learn (with resources)
- **CS50P** — Week 2 (Loops), the sections covering lists and iteration.
- **Corey Schafer** — "Python Tutorial: Lists, Tuples, and Sets" video (YouTube).
- **Python docs** — Data Structures tutorial: https://docs.python.org/3/tutorial/datastructures.html

## Homework (branch → PR)
1. [ ] Branch: `git switch -c day-02-lists-tuples`.
2. [ ] `projects/small/collections/list_ops.py`: build a list of numbers, then produce (a) a sorted copy with `sorted()`, (b) an in-place sort, (c) the list sorted descending, and (d) sorted by a key (e.g. list of `(name, score)` tuples sorted by score). Print each result.
3. [ ] `projects/small/collections/grid.py`: build a nested list (a 3×3 grid), print it row by row, and write a function `flatten(grid)` that returns a single flat list.
4. [ ] `projects/small/collections/comprehensions.py`: rewrite three simple loops as list comprehensions — squares of 1–10, even numbers only (with `if` filter), and uppercased words from a sentence.
5. [ ] Add a one-line comment next to one comprehension explaining why a loop might read better if it grew more complex.
6. [ ] CS50P Week 2 problem set: complete at least one list-based exercise.
7. [ ] Push, PR with `Closes #<issue>`, self-review, merge.

## Reflection
- **What clicked:**
- **What's still fuzzy:**
- **One thing to review tomorrow:**
- **Time actually spent:**
